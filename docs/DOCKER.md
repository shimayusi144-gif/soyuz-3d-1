# 🐳 Docker

## Разработка

```bash
# Запустить приложение в контейнере
docker-compose up

# Запустить в фоне
docker-compose up -d

# Остановить
docker-compose down

# Пересобрать образ
docker-compose up --build
```

## Продакшен

```bash
# Собрать образ
docker build -t soyuz-3d:latest .

# Запустить контейнер
docker run -p 3000:3000 soyuz-3d:latest

# С переменными окружения
docker run -p 3000:3000 -e NODE_ENV=production soyuz-3d:latest
```

## Структура Dockerfile

1. **Builder stage** - сборка приложения с Vite
2. **Production stage** - минималистичный образ только с production зависимостями

Это снижает размер финального образа с ~500MB до ~150MB

## Environment переменные

- `NODE_ENV` - окружение (development/production)
- `PORT` - порт сервера (по умолчанию 3000)

## Логирование

```bash
# Просмотреть логи контейнера
docker logs soyuz-3d

# Логи в реальном времени
docker logs -f soyuz-3d
```
