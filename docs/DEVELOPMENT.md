# 👨‍💻 Разработка

## Установка

```bash
npm install
```

## Локальная разработка

```bash
npm run dev
```

Откроется на http://localhost:5173 с горячей перезагрузкой.

## Структура проекта

```
3d_website/
├── assets/                 # Статические файлы
│   ├── css/               # Стили (styles.css)
│   ├── js/                # JavaScript (cookie-handler.js)
│   └── images/            # Изображения
├── pages/                 # HTML страницы сервисов
│   ├── 3d-printing.html
│   ├── 3d-modeling.html
│   ├── reverse-engineering.html
│   └── cookie-policy.html
├── docs/                  # Документация
├── index.html             # Главная страница
├── package.json           # Dependencies
├── vite.config.js         # Vite конфигурация
├── Dockerfile             # Docker конфигурация
├── docker-compose.yml     # Docker Compose
├── server.js              # Продакшен сервер
└── README.md              # Основная документация
```

## Команды

```bash
# Разработка с HMR
npm run dev

# Продакшен сборка
npm run build

# Просмотр собранного проекта
npm run preview

# Запуск продакшен сервера
npm run serve
```

## Контрибьютинг

1. Создать новую ветку: `git checkout -b feature/название`
2. Сделать изменения
3. Протестировать: `npm run build`
4. Запушить: `git push origin feature/название`
5. Создать Pull Request

## Стилизация

- CSS переменные используются для цветов (--black, --white, --gray)
- Responsive design с breakpoint 768px
- Google Fonts: DM Sans и DM Serif Display

## JavaScript

- Vanilla JS без фреймворков
- Cookie консент система в `assets/js/cookie-handler.js`
- Smooth scrolling для якорей

## Производительность

- Vite для быстрой разработки
- Minification и хеширование в продакшене
- Файлы автоматически организуются в dist/assets/

## Браузеры

- Chrome, Firefox, Safari, Edge (последние 2 версии)
- Mobile: iOS Safari 12+, Chrome Android 90+
